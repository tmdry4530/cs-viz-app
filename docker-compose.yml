services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5434:5432"
    environment:
      POSTGRES_DB: csviz
      POSTGRES_USER: csviz
      POSTGRES_PASSWORD: csviz_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
